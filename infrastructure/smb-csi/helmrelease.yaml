apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: smb-csi-driver
  namespace: kube-system
spec:
  interval: 30m
  chart:
    spec:
      chart: csi-driver-smb
      version: "v1.17.0"
      sourceRef:
        kind: HelmRepository
        name: smb-csi-driver
        namespace: flux-system
  values:
    controller:
      replicas: 1
