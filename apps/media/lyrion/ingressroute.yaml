apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: lms
  namespace: media
spec:
  entryPoints:
    - lms-web
  routes:
    - match: PathPrefix(`/`)
      kind: Rule
      services:
        - name: lyrion-main
          port: 9000
