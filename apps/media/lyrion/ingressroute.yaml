apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: lms
  namespace: media
spec:
  entryPoints:
    - lms-web
  routes:
    - match: PathPrefix(`/`)
      kind: Rule
      services:
        - name: lyrion
          port: 9000
